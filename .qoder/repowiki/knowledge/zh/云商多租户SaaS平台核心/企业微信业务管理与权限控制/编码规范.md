- 控制器统一返回标准化 JSON 格式，通过 `successJson` 和 `errorJson` 方法封装响应结构。
- 服务类（Service）采用静态方法调用模式，内部通过统一的 `returnArr` 方法返回包含 result、msg、data 的结果数组。
- 模型类通过 `business()` 作用域方法自动注入当前租户 ID（business_id）以实现数据隔离。
- 菜单与权限配置采用声明式数组定义，通过 `type` 字段区分页面节点与接口路由，并支持多级嵌套。