- 全局辅助函数采用 `function_exists` 包裹定义，防止在模块化或插件化加载过程中产生命名冲突。
- 多端路由与资源路径通过统一的 URL 辅助函数族（如 `yzWebUrl`, `yzApiUrl`, `yz_tomedia`）进行抽象，根据 `config('app.framework')` 动态切换平台模式或插件模式的路径前缀。
- 业务上下文状态（如当前用户 ID、公众号 Uniacid）通过单例模式的 `YunShop::app()` 及其属性访问器统一获取，避免直接依赖全局变量 `$_W`。