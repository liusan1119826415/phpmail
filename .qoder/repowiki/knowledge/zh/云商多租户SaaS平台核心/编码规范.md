- 后端业务逻辑严格遵循 Laravel 的 MVC 分层，复杂流程通过 `app/Jobs` 队列化或 `app/Services` 服务类封装。
- 前端静态资源按端隔离，`static/app` 存放移动端 H5 资源，`static/framework` 存放管理后台资源，均通过哈希版本号实现缓存控制。
- 数据库迁移文件（`database/migrations`）采用时间戳前缀命名，确保多环境下的 schema 演进一致性。